# t3st - RTS scaffold (SDL3, C)

Minimal workspace scaffold for an RTS-style project in plain C.
Use CMake + SDL3.

Build (example):
mkdir build
cd build
cmake .. -G "Visual Studio 17 2022"
cmake --build . --config Release
